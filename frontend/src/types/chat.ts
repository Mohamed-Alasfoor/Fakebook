export interface Message {
    id: string
    senderId: string
    text: string
    timestamp: string
  }
  
  export interface User {
    id: string
    name: string
    online: boolean
    avatar: string
  }
  
  